# Phase 18 — 의미 질의와 설명 계층

> 그래프를 사람이 사용할 수 있는 답으로 바꾼다.

## 목표

Phase 10의 역량 질문을 CLI 또는 Python API에서 실행하고, 결과와 함께 선택 이유·
관계 경로·원문 근거를 반환한다. SPARQL 문자열을 노출하는 것만으로는 운영 기능이 아니다.

## Step 1 — 질문 API를 정의한다

초기 명령 후보:

```text
lineage       사건 → 주장 → 원칙 → 절차 계보
evidence      원칙·결정의 근거
conflicts     충돌하는 주장과 영향 대상
impact        원칙·근거 변경의 영향 범위
review        현재 재검토 후보와 이유
explain       특정 결과의 derivation
cross-domain  경험·철학·개발·커리어 사이의 근거 있는 접점
```

명령 이름은 구현 시 기존 CLI와 충돌 여부를 확인해 확정한다. 범용 SPARQL 실행보다
반복 사용하는 질문을 안정된 계약으로 먼저 제공한다.

### 확정 — `ask` 여섯 개 (2026-08-31)

기존 `q`(문서 그래프)와 나란히 `ask`(의미 그래프)를 둔다. 충돌 없음.

```
vault ask lineage  <note>    이 판단 뒤의 근거 사슬 전부      C02 · C04 · C09 · C15
vault ask evidence <note>    바로 앞의 근거 한 홉             C02
vault ask affected <note>    이걸 고치면 무엇이 영향받나        C21 · C29
vault ask review             무효화된 근거에 선 것             C03 · C30
vault ask crossing           삶과 일 사이를 오간 사슬          C07 · C09 · C15
vault ask together           같은 근거를 공유하는 것들          C05 · C06
```

`explain`은 별도 명령으로 안 만들었다. **모든 답이 이미 경로다** — Phase 17이 규칙을
endpoint가 아니라 path를 돌려주게 만들었으므로, 설명을 따로 물을 자리가 없다.

`lineage`와 `evidence`를 나눈 이유 — 「무엇에 기대고 있나」의 답은 **이웃**이고,
3홉 사슬을 돌려주면 그 답이 더 긴 답 안에 묻힌다.

## Step 2 — 답의 구조를 고정한다

각 결과에는 다음을 포함한다.

- 지식 개체의 사람이 읽는 이름과 ID
- 연결된 Markdown 경로와 section/block
- 관계 종류와 방향
- asserted·inferred·proposed 상태
- 출처와 승인 정보
- 결과가 나온 관계 경로
- 불확실하거나 누락된 정보
- 주관적 진술의 관점 주체와 당시/현재 유효 시점

"없음"과 "아직 기록되지 않음"을 구분한다. 열린 세계에서 조회 결과가 없다고 사실의
부재를 단정하지 않는다.

교차 영역 답은 연결 수를 많이 보여주는 것이 목표가 아니다. 예를 들어 “개인 경험이
어떤 개발 원칙에 영향을 주었는가?”라는 질문에는 승인된 `motivates`·`derivedFrom`
경로와 양쪽 원문을 제시한다. 의미 유사 후보만 있으면 확정 답이 아니라 별도 후보로
표시한다.

### 「없음」과 「아직 안 적힘」을 넷으로 갈랐다

이 모듈이 SPARQL 문자열 대신 존재하는 이유다. 질의가 0행을 돌려주는 것과 사실이
없는 것은 다르고, **`status`가 그 차이를 말한다.**

| status | 뜻 | 실측 |
|---|---|---|
| `answered` | 적혀 있다 | 문서 138 |
| `lived` | 출처가 문서가 아니라 겪은 일이다 | 문서 16 |
| `unrecorded` | **아직 안 적혔다 — 없다는 뜻이 아니다** | 문서 4,896 |
| `searched` | 찾아봤고 없었다 | **0** — 어휘만 있다 |

`lived`가 따로 있는 이유가 이 절의 전부다. `derived_from: experience`는 **대답**이다.
「출처가 문서가 아니다」라고 말하고 있는데, 이걸 `unrecorded`로 접으면 **한 말이
침묵으로 바뀐다.**

`searched`는 `v:source_unknown`이 갈 자리인데 vault에 0건이고 빌더도 안 내보낸다.
**어휘만 두고 값은 안 만든다** — 데이터가 생기는 날 앉을 자리다.

### 답은 rdflib 을 안 흘린다

`Answer`와 `Hop`이 전부 `str`이다. Step 4가 요구하는 「저장 엔진을 숨긴다」를
**약속이 아니라 타입으로** 지불했다.

```python
Hop(source="400 …/Decision Node - JWT vs Session.md", source_heading="",
    relation="derived_from",
    target="400 …/Trade-offs - JWT vs Session.md", target_heading="")
```

`target_heading`이 따로 있어서, 답이 「이 파일 어딘가」가 아니라
`_Insights.md#인사이트 20`까지 가리킨다.

## Step 3 — gold answer 회귀 테스트를 만든다

Phase 12의 50개 문서와 Phase 10의 기대 답을 연결한다.

- 정확한 결과 집합
- 관계 경로
- 제외되어야 할 제안
- 설명에 포함될 근거
- 결과가 없어야 하는 반례

텍스트 출력 전체 snapshot보다 구조화된 answer object를 먼저 테스트한다.

### 실측 (2026-08-31 · `tools/measure_phase18.py`)

```
gold answer 회귀   33 되찾음 · 0 놓침

질문별
  together    14 경로   answered
  review       0 경로   unrecorded
  crossing     0 경로   unrecorded

문서 5,050개에 전부 물어보기
  lineage    answered 138 · lived 16 · unrecorded 4,896   0.10s
  affected   answered 143 · unrecorded 4,907              0.04s
```

**Phase 12가 손으로 쓴 관계 33건을 `ask evidence`가 하나도 안 놓친다.**

### 이 Phase의 큰 발견 — 교차 영역 사슬이 0건이다

```
의미 관계를 가진 문서, 대역별
   120  200 Dev Knowledge Base    기술
    21  400 Logic Forge           기술
     7  300 Runtime               기술
     3  100 Private Log           개인
     1  500 Mind Compiler         개인

개인 ↔ 기술 사슬   0
```

**C09(개인 기준이 기술 결정에)와 C15(반응이 성찰이나 창작으로)는 데이터가 없다.**
Phase 10이 3부의 정당성을 「문서보다 작은 단위가 필요한가」에 걸었고, Phase 12~17이
그걸 다 만들었는데, **정작 교차 영역은 라벨링이 기술 쪽에만 붙었다.**

`crossing`을 「아무 두 대역」으로 두면 163개 사슬 중 145개가 걸린다. 그 중 103개가
200↔300, 즉 **기술에서 기술로** 간다. 이 Phase가 스스로 「연결 수를 많이 보여주는
것이 목표가 아니다」라고 적었으므로, **개인 대역과 기술 대역을 이름으로 갈라 놓고**
둘 다 닿는 사슬만 센다. 그랬더니 0이다.

**0이 실패가 아니다.** `unrecorded`가 「아직 안 적혔다」라고 말하고, 그것이 Step 5의
2주 동안 무엇을 라벨링해야 하는지를 가리킨다.

## Step 4 — 저장 엔진을 숨긴다

질문 API가 rdflib 구현 세부에 직접 묶이지 않도록 한다. SQLite·SPARQL·Python rule 중
어느 경로가 적합한지는 질문별 실측으로 결정한다. 모든 것을 RDF 엔진으로 옮기는 것이
목표가 아니다.

### 세 층이 각자 다른 것을 답한다

```
SQLite (q)      문서 그래프 — 어디에 있나, 무엇에 링크하나        상시 운용 (2부 결정)
rdflib (ask)    의미 그래프 — 무엇에 기대고 있나                Phase 15~18
Python (rules)  경로 — 어떻게 거기까지 왔나                     Phase 17
```

**아무것도 옮기지 않았다.** `q`는 SQLite 위에서 그대로 돌고, `ask`는 그 옆에 섰다.
2부의 결론(「상시 운용은 SQLite」)이 유지되고, 의미 질의만 RDF를 쓴다.

경계는 답의 타입이 지킨다 — `ask`가 `str`만 돌려주므로 아래를 바꿔도 위가 모른다.

## Step 5 — 실제 사용 시험을 한다

최소 2주 동안 다음을 기록한다.

- 어떤 질문을 몇 번 실행했는가
- 기존 grep·Dataview·`q near/path`로 충분했는가
- 답을 실제 결정이나 문서 수정에 사용했는가
- 설명을 신뢰할 수 있었는가
- 잘못된 결과를 수정하는 데 얼마나 걸렸는가
- 교차 영역 연결이 새로운 성찰이나 실제 판단으로 이어졌는가

### 축이 셋이다

```
① 19·20 을 할 가치가 있나          기존 축
② 디렉토리 구성을 바꿔야 하나       2026-08-31 추가
③ 개인 대역 라벨링이 쓸모가 있나    2026-09-01 추가 — 아래
```

**③은 하루 사이에 질문이 뒤집혔다.** 2026-09-01 오전에 `ask crossing`이 **0**이었고
그것이 3부의 가장 큰 구멍이었다 — 의미 관계를 가진 문서 148개 중 개인 대역이 4개라
C09·C15(개인 경험이 기술 판단에)를 답할 데이터가 없었다. 같은 날 저녁 전수 연결이
개인 대역을 **33개**로 늘리면서 사슬 **26개**가 생겼다.

```
개발자라는 직업에 있어서 나의 약점은 무엇인가?
    --answered_by-->  코드 품질을 높이는 구체적인 방법은?
    --answered_by-->  코드 품질 개선 리팩토링 전략 (302.6 M-Project Learnings)
```

그래서 축이 **「메울 것인가」에서 「메웠는데 쓸모가 있나」**로 바뀌었다.

2026-08-31 vault 정합화 세션이 **「스키마가 경로를 정한다」로 뒤집지 않는다**로 결론
냈다. 경로(삶의 자리)와 `type`(읽는 방식)은 직교라 중복이 아니고, 300의 판별력 0은
경로 탓이 아니라 종류 축의 빠진 값(`case`) 탓이었다는 판정이다. 그 세션은 **디렉토리
이동 0건**으로 끝났고 재편은 관찰 뒤로 미뤘다.

**그 관찰을 여기서 한다.** Phase 18이 실제 질의를 돌려주므로, 질문에 답이 안 나올 때
원인이 어디인지 처음으로 실물로 갈린다.

```
답이 안 나온다  →  사실이 안 적혀 있다        →  작성 계약의 문제
                →  적혀 있는데 못 찾는다      →  어휘·추론의 문제
                →  경로가 의미를 흩어 놨다     →  ★ 디렉토리의 문제
```

기록할 것.

- 한 질문의 답이 **여러 대역에 흩어져** 있어 경로가 방해가 된 사례
- `type`으로는 갈리는데 **폴더로는 안 갈리는** 조회 (또는 그 반대)
- `Incident Response/` 처럼 **폴더가 관행으로 유지된 자리**가 실제로 질의를 방해했는가
- `project-doc` 786개가 한 덩어리인 것이 답을 뭉갠 횟수

근거와 미결 항목은 vault의 `000 Index/Dots/전 디렉토리 정합화 — 마스터 플랜.md`와
`ontology → vault TODO — 사건 문서와 프로젝트 관리.md`에 있다. **여기서 나온 관측을
그 문서로 돌려보낸다.**

---

## 재출발 — 2026-09-08

09-01 에 연 창(~09-14)은 접는다. 그 사이 재는 대상이 바뀌었다 — `vault new` 라우팅 경고,
`vo`, 그리고 vault-vector 가 필터·증분·배치·판정·그래프 확장을 갖췄다. 새 창은 **09-08 ~ 09-22**,
출발선 숫자는 vault 노트 「의미 질의 2주 실사용 관찰」의 재출발 절에, 질의 기록은 `$VAULT_ASK_LOG`
(위 NEXT.md 「환경 복원」)와 vault-vector 의 `index/absent.jsonl` 이 자동으로 남긴다. 아래 판정
절차는 그대로다 — `<시작커밋>` 만 재출발 커밋으로 읽는다.

## 2주 뒤 — 무엇을 보고 어떻게 정하는가

> **이 절이 판정의 정본이다.** vault의 기록장
> (`000 Index/Dots/의미 질의 2주 실사용 관찰.md`)은 재료를 모으고, 판정은 여기서 한다.

### 절차 — 30분

```bash
cd ~/Desktop/Code/vault-ontology
uv run python -m vault rdf                    # 그래프를 새로 만든다
uv run python -m tools.measure_phase18        # 숫자를 다시 잰다
uv run python -m vault validate --audit       # 계약 상태
uv run python -m vault ask crossing           # ③번 축의 실물

cd "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian Vault"
git diff --stat <시작커밋>..HEAD              # 2주 동안 무엇이 움직였나
```

셸 히스토리를 켜 뒀다면 명령 기록도 함께 되살린다.

```bash
awk -F';' '/vault (ask|validate)/{split($1,a,":");
  if(a[2]!="") print strftime("%Y-%m-%d %H:%M", a[2]) "  " $2}' ~/.zsh_history
```

### 판정 — 축마다 증거와 결론이 짝지어져 있다

**세지 않으면 판정이 아니다.** 아래 숫자를 먼저 채우고 표를 읽는다.

```
A  2주 동안 `ask` 를 몇 번 썼나            (히스토리 또는 기록장)
B  답을 보고 실제로 파일을 고친 횟수         (기록장)
C  「경로가 의미를 흩어 놨다」로 분류된 건수  (기록장의 원인 표)
D  `ask crossing` 사슬을 읽은 횟수 · 거기서 나온 판단  (기록장)
E  의미 관계 수의 변화                     (measure_phase18, 출발선 275)
```

#### ① Phase 19·20

| 증거 | 결론 |
|---|---|
| **A ≤ 5** | **보류 유지.** 제안을 자동으로 만들어도 볼 사람이 없다. Step 5의 「검토 비용」이 그 말이다 |
| A ≥ 15 이고 **「관계가 더 있었으면」이 기록장에 반복** | **19 재개.** 손으로 다는 것이 병목이라는 뜻 |
| **B = 0** | **20 보류 유지.** 답이 파일을 안 고치는데 파일을 고칠 권한을 만들 이유가 없다 |
| B ≥ 3 이고 그 수정이 기계적 | 20 재검토. shadow mode부터 |

**A가 작으면 그것으로 끝난다.** 19·20은 「질의가 자주 쓰인다」를 전제로 서 있고,
전제가 없으면 두 Phase 다 답이 정해진다.

#### ② 디렉토리

| 증거 | 결론 |
|---|---|
| **C = 0** | **안 바꾼다.** 2026-08-31 정합화 세션의 「경로와 `type`은 직교」가 실사용으로 확인된 것 |
| C 가 1~2 이고 서로 다른 자리 | **안 바꾼다.** 개별 교정으로 처리하고 마스터 플랜 「관찰 항목」에 적는다 |
| **C ≥ 3 이고 같은 대역에 몰림** | **그 대역만 연다.** 전면 개편이 아니라 `project-doc` 786 같은 특정 덩어리다 |
| C 가 여러 대역에 흩어짐 | 마스터 플랜을 다시 연다. **이때만 「대개편」이 후보가 된다** |

`schema-change-impact.md`의 「범위를 좁혀주는 구조가 있는가」를 먼저 본다 —
경로·파일명·`type` 중 하나가 대상을 결정론적으로 집으면 전역 작업이 아니다.

#### ③ 개인 대역 라벨링

| 증거 | 결론 |
|---|---|
| **D = 0** | **더 안 붙인다.** 26개 사슬을 한 번도 안 읽었으면 만들어도 안 본다는 뜻 |
| D ≥ 1 이지만 **판단이 안 나옴** | 붙이는 것을 멈추고 **어휘를 의심한다.** `answered_by`가 맞는 관계였나 |
| D ≥ 1 이고 **실제 판단이 나옴** | **100·500 라벨링을 계속한다.** C07·C09·C15가 값을 한다는 증거 |

**③은 A와 다르다.** `ask crossing`은 한 번 읽어서 무언가 나오면 그것으로 값을 한다.
횟수가 아니라 **한 번의 수확**이 기준이다.

### 결론을 적는 자리

판정이 끝나면 **이 절 아래에 날짜와 함께 적는다.** 세 축 각각 한 문단이면 된다.
그리고 그 결론이 다음을 정한다.

```
① →  phase19.md · phase20.md 를 되살릴지
② →  vault 마스터 플랜의 「관찰 항목」을 열지
③ →  전수 연결을 개인 대역으로 계속할지
```

**셋 다 「안 한다」로 끝나도 실패가 아니다.** Phase 14가 클래스 13개 중 11개를,
Phase 15가 provenance 8항목 중 7개를, Phase 17이 물질화를 죽였다. 개수가 정하게
두는 것이 이 프로젝트의 방식이다.

---

## 판정 결과

> 2주 실사용 뒤 여기에 적는다. 비어 있으면 아직 안 했다는 뜻이다.

## Vector 경계

VectorDB는 이 Phase에서 구현하지 않는다. 나중에 연결할 수 있도록 결과 후보가 다음
식별자를 받을 계약만 둔다.

```text
chunk_id → artifact IRI → section/block → knowledge entity
```

Vector 점수는 검색 신호이며 asserted graph에 들어가지 않는다.

## 산출물

- semantic query API/CLI
- 구조화된 answer와 explain 모델
- competency question 회귀 테스트
- 2주 사용 로그와 기존 도구 대조
- Vector 연동 인터페이스 문서

## 완료 조건

- [ ] 우선 역량 질문이 명령 또는 API로 실행된다.
- [ ] 모든 답에서 원문과 관계 근거로 이동할 수 있다.
- [ ] proposed 사실이 기본 답에 섞이지 않는다.
- [ ] 없음과 미기록을 구분한다.
- [ ] 기존 도구로 충분한 질문을 중복 구현하지 않는다.
- [ ] 최소 2주간 실제 사용 결과가 있다.
- [ ] 개발·개인·철학을 잇는 질문에서 근거 없는 연결을 확정 답으로 내지 않는다.

## 난이도와 위험

**난이도: 높음. 정확한 그래프를 유용한 답으로 바꾸는 제품 설계 단계다.**

SPARQL이 정답을 반환해도 사람이 이유를 이해하지 못하면 사용할 수 없다. 반대로
자연어 설명이 매끄러워도 근거 경로가 틀리면 위험하다. 구조화된 답을 정본으로 두고
자연어는 그 위의 표현 계층으로만 만든다.

## 완료 판정 (2026-08-31)

| | |
|---|---|
| 역량 질문을 CLI에서 실행한다 | `vault ask` 여섯 개 |
| 답에 관계 경로와 원문 근거가 붙는다 | 모든 답이 경로다. `경로#제목`까지 |
| 「없음」과 「아직 안 적힘」을 구분한다 | `answered` · `lived` · `unrecorded` (+`searched` 예약) |
| gold answer 회귀 | **33 / 33** |
| 저장 엔진이 안 새어 나온다 | `Answer`·`Hop`이 전부 `str` |
| 실사용 시험 | **Step 5. 2주. 아직 안 함** |

**3부의 구현은 여기서 끝난다.** 남은 것은 쓰는 일이고, 그것이 Step 5다.
